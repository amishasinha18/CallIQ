name: keep-alive

# Render's free web services spin down after ~15 minutes of no traffic and
# take 30-60s to wake back up. Pinging both services every 10 minutes resets
# that idle timer before it fires. Repo is public, so Actions minutes here
# are unlimited/free.

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 75 https://calliq-t8ut.onrender.com/health) || true
          echo "backend /health -> $code"

      - name: Ping frontend
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 75 https://calliq-web-mfv4.onrender.com/) || true
          echo "frontend / -> $code"
